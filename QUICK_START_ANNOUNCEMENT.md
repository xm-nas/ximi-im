# 🚀 公告系统快速开始指南

## 5分钟快速上手

### 第一步：系统已自动集成
系统更新后，公告系统已自动集成到所有模块中，无需额外配置。

### 第二步：管理员创建公告

1. **登录管理后台**
   - 访问 `admin.php`
   - 输入超级管理员密码登录

2. **进入公告系统**
   - 点击菜单中的 "📢 公告系统" 标签页

3. **创建新公告**
   - 填写公告标题（必填）
   - 填写公告内容（必填）
   - 设置优先级（可选，默认0）
   - 设置状态：发布中 / 已下线
   - 设置有效期（可选，留空表示永久）
   - 点击 "创建公告" 按钮

### 第三步：用户查看公告

1. **自动显示**
   - 用户登录后，公告自动显示在页面顶部
   - 自动标记为已读

2. **关闭公告**
   - 点击公告面板右上角的 ✕ 按钮关闭
   - 5分钟后自动检查新公告

3. **公告颜色说明**
   ```
   🔴 红色背景  = 高优先级 (priority > 5)
   🟠 橙色背景  = 中优先级 (priority > 2)
   🔵 蓝色背景  = 普通公告 (priority ≤ 2)
   ```

## 📋 常见操作

### 创建公告
```
标题：系统升级公告
内容：系统将于明天凌晨进行升级，预计2小时完成
优先级：3
状态：发布中
有效期：2024-12-31 06:00:00
```

### 编辑公告
1. 在公告列表中找到要编辑的公告
2. 点击 "编辑" 按钮
3. 修改内容
4. 点击 "更新公告" 按钮

### 删除公告
1. 在公告列表中找到要删除的公告
2. 点击 "删除" 按钮
3. 确认删除

### 下线公告
1. 编辑公告
2. 将状态改为 "已下线"
3. 点击 "更新公告"

## 🔍 API 快速调用

### 获取公告列表
```javascript
fetch('api.php?action=get_announcements&user_id=1')
    .then(res => res.json())
    .then(data => {
        if (data.code === 200) {
            console.log('公告列表:', data.data);
        }
    });
```

### 创建公告
```javascript
fetch('api.php?action=create_announcement', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        creator_id: 1,
        title: '重要公告',
        content: '这是一条重要公告',
        priority: 5,
        end_time: '2024-12-31 23:59:59'
    })
})
.then(res => res.json())
.then(data => console.log(data));
```

## ⚙️ 常见问题

### Q1: 公告不显示怎么办？
**A:** 检查以下几点：
- [ ] 是否有创建状态为 "active" 的公告
- [ ] 公告是否已过期（end_time）
- [ ] 是否登录了
- [ ] 浏览器是否支持现代 CSS
- [ ] 是否清除了浏览器缓存

### Q2: 如何让公告一直显示？
**A:** 有效期留空即可，这样公告会永久有效。

### Q3: 如何设置公告优先级？
**A:** 优先级是数字形式，越大越靠前：
- 9-10：紧急公告（红色）
- 3-8：重要公告（橙色）
- 0-2：普通公告（蓝色）

### Q4: 已读状态存储在哪里？
**A:** 存储在 `announcement_reads` 表中，记录用户ID和阅读时间。

### Q5: 能否删除已读记录？
**A:** 删除公告时会自动清理相关的已读记录。

## 📊 示例：完整的公告流程

### 场景：系统维护公告

**步骤 1：管理员创建公告**
```
标题: 系统维护通知
内容: 由于系统升级，平台将于12月15日22:00-24:00停止服务，给您带来不便敬请谅解
优先级: 8
状态: 发布中
有效期: 2024-12-15 23:59:59
```

**步骤 2：系统发送通知**
- 所有登录用户会看到 🔴 红色公告面板
- 包含标题和完整内容
- 自动标记该用户为已读

**步骤 3：维护完成后**
- 管理员编辑公告，将状态改为 "已下线"
- 或直接删除公告

## 🎨 自定义公告样式

如需自定义公告面板样式，编辑 `index.html` 中的 `announcementPanel` div：

```html
<div id="announcementPanel" class="hidden fixed top-0 left-0 right-0 z-[4999] 
    bg-gradient-to-r from-blue-500 to-blue-600 text-white shadow-lg max-h-screen overflow-y-auto">
    <!-- 修改类名可改变样式 -->
</div>
```

## 📱 移动端适配

公告系统已完全适配移动设备：
- 响应式设计
- 触摸友好的按钮
- 自动调整字体大小

## 🔐 权限说明

| 操作 | 需要认证 | 说明 |
|------|--------|------|
| 查看公告 | ❌ | 未登录也能看到 |
| 创建公告 | ✅ | 需要用户认证 |
| 编辑公告 | ✅ | 需要用户认证 |
| 删除公告 | ✅ | 需要用户认证 |
| 标记已读 | ✅ | 需要登录用户 |

## 💾 数据备份

建议定期备份公告数据：

```sql
-- 导出公告和已读记录
SELECT * FROM announcements;
SELECT * FROM announcement_reads;
```

## 📈 性能优化

公告系统已优化以下性能指标：
- ✓ 自动过滤过期公告，减少查询
- ✓ 已读状态使用 UNIQUE 约束，避免重复
- ✓ 每5分钟检查一次，不会频繁查询
- ✓ 前端缓存管理，减少服务器负载

## 🆘 需要帮助？

- 📖 详见 `ANNOUNCEMENT_SYSTEM.md` 完整文档
- 🔧 详见 `ANNOUNCEMENT_IMPLEMENTATION.md` 实现细节
- 💬 查看代码注释了解具体实现

## ✅ 检查清单

安装完成后，请检查以下项目：

- [ ] admin.php 中有 "📢 公告系统" 菜单
- [ ] 能成功创建公告
- [ ] 登录后能看到公告面板
- [ ] 公告能正确关闭
- [ ] 不同优先级显示不同颜色
- [ ] 公告定期更新（每5分钟）

---

**祝您使用愉快！** 🎉
